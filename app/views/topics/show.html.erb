<main role="main" class="container">
  <h1><%= @topic.title %></h1>
  
  <div class="row">
    <div class="col-md-8 blog-main">
      <%= paginate @blogs %>

      <%= render @blogs %>

      <%= paginate @blogs %>
    </div>

    <div class="col-md-4">
      <%= render 'shared/blog_sidebar' %>
    </div>
  </div>
</main>
