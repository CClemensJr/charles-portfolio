<% unless current_user.is_a? GuestUser %>
  <%= form_with url: '#' do |form| %>
    <div class="form-group">
      <%= form.text_area :content, class: 'form-control', placeholder: 'Your comment' %>
    </div>

    <%= form.submit 'Post Comment', class: "btn btn-primary" %>
  <% end %>
<% end %>
